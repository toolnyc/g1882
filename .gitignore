build
dist / media
node_modules
.DS_Store
.env
.env*.local
.next
.vercel
.sentryclirc

# Claude agentic state (machine-local, ephemeral)
.claude/state/
.claude/worktrees/
# Vendor skills (reinstallable via npx skills add from skills-lock.json)
# Custom skills already tracked are unaffected by this rule.
.claude/skills/
.agents/skills/.anthropics_skills_installed
.agents/skills/.trailofbits_skills_installed

# Build artifacts
tsconfig.tsbuildinfo

# Payload default media upload directory
public/media/

public/robots.txt
public/sitemap*.xml


# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
